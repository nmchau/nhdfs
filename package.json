{
  "name": "nhdfs",
  "version": "0.0.2",
  "description": "ode.js Native HDFS library wrapper",
  "homepage": "https://github.com/timout/nhdfs",
  "repository": {
    "type": "git",
    "url": "https://github.com/timout/nhdfs.git"
  },
  "main": "./lib/nhdfs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "preinstall": "./hdfslib-build.sh",
    "install": "node-gyp rebuild",
    "gyp-build": "node-gyp rebuild",
    "clean": "rm -rf build_deps"    
  },
  "author": "timout",
  "license": "MIT",
  "keywords": [
    "HDFS",
    "libhdfs3"
  ],
  "dependencies": {
    "bindings": "^1.2.1",
    "node-addon-api": "^0.3.5"
  },
  "devDependencies": {
    "node-gyp": "^3.6.2"
  }
}
